% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert.R
\name{bam_convert_bed}
\alias{bam_convert_bed}
\title{Convert BAM/CRAM to a bgzipped BED bin-count file}
\usage{
bam_convert_bed(
  bam,
  bed,
  binsize = 5000L,
  rmdup = c("streaming", "none", "flag"),
  con = NULL,
  reference = NULL,
  index = TRUE
)
}
\arguments{
\item{bam}{Path to an indexed BAM or CRAM file.}

\item{bed}{Path for the output \code{.bed.gz} file (created or overwritten).
The tabix index is written to \code{paste0(bed, ".tbi")}.}

\item{binsize}{Bin size in base pairs (default 5000).}

\item{rmdup}{Duplicate-removal strategy passed to \code{\link[=bam_convert]{bam_convert()}}.}

\item{con}{Optional open DBI connection with duckhts already loaded.
If \code{NULL} (default), a temporary in-memory DuckDB connection is created.}

\item{reference}{Optional FASTA reference path for CRAM inputs.}

\item{index}{Logical; when \code{TRUE} (default) a tabix index is created
alongside the bgzipped output.}
}
\value{
\code{bed} (invisibly).
}
\description{
Runs \code{\link[=bam_convert]{bam_convert()}} and writes the per-bin read counts to a bgzipped,
tab-delimited BED file (four columns: \code{chrom}, \code{start}, \code{end}, \code{count}),
then creates a tabix index (\code{.tbi}) alongside it via Rduckhts.
This is the language-agnostic intermediate format for the RWisecondorX
native pipeline; the file can be queried directly with duckhts/DuckDB or
any tabix-aware tool.
}
\details{
Coordinates are 0-based half-open intervals matching the BED convention
(\code{start = bin_index * binsize}, \code{end = start + binsize}). Chromosomes are
written as \code{1}–\code{22}, \code{X}, \code{Y} (no \code{chr} prefix) in numeric order. All
bins are written, including those with a count of zero.

bgzip and tabix indexing are performed via \code{\link[Rduckhts:rduckhts_bgzip]{Rduckhts::rduckhts_bgzip()}} and
\code{\link[Rduckhts:rduckhts_tabix_index]{Rduckhts::rduckhts_tabix_index()}}, so no external tools are required.
}
\examples{
\dontrun{
bam_convert_bed("sample.bam", "sample.bed.gz", binsize = 5000, rmdup = "streaming")
}

}
\seealso{
\code{\link[=bam_convert]{bam_convert()}}, \code{\link[=bam_convert_npz]{bam_convert_npz()}}, \code{wisecondorx_convert()}
}

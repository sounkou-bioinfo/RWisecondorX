% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wisecondorx_npz.R
\name{bam_convert_npz}
\alias{bam_convert_npz}
\title{Convert BAM/CRAM to WisecondorX NPZ format}
\usage{
bam_convert_npz(
  bam,
  npz,
  binsize = 5000L,
  mapq = 1L,
  require_flags = 0L,
  exclude_flags = 0L,
  rmdup = c("streaming", "none", "flag"),
  con = NULL,
  np = NULL,
  reference = NULL
)
}
\arguments{
\item{bam}{Path to an indexed BAM or CRAM file.}

\item{npz}{Path for the output \code{.npz} file (created or overwritten).}

\item{binsize}{Bin size in base pairs (default 5000).}

\item{mapq}{Minimum mapping quality passed to \code{\link[=bam_convert]{bam_convert()}}.}

\item{require_flags}{Integer bitmask of required SAM flags passed to
\code{\link[=bam_convert]{bam_convert()}}.}

\item{exclude_flags}{Integer bitmask of excluded SAM flags passed to
\code{\link[=bam_convert]{bam_convert()}}.}

\item{rmdup}{Duplicate-removal strategy passed to \code{\link[=bam_convert]{bam_convert()}}.}

\item{con}{Optional open DBI connection with duckhts already loaded.
Passed through to \code{\link[=bam_convert]{bam_convert()}}.}

\item{np}{Optional numpy module imported via \code{reticulate::import("numpy")}.
If \code{NULL} (default) it is imported automatically.}

\item{reference}{Optional FASTA reference path for CRAM inputs. Passed to
\code{\link[=bam_convert]{bam_convert()}}.}
}
\value{
\code{npz} (invisibly).
}
\description{
Runs \code{\link[=bam_convert]{bam_convert()}} and serialises the resulting bin-count list to a
\code{.npz} file that is byte-compatible with the file written by
\verb{wisecondorx convert}.  The NPZ must be created by numpy so that
\verb{wisecondorx newref} can load it; this function therefore requires
\code{reticulate} and a Python environment with \code{numpy} installed; any numpy
version from 1.16 onward works.
}
\details{
This function exists for Python WisecondorX CLI conformance and
interoperability. The native R pipeline uses \code{\link[=bam_convert_bed]{bam_convert_bed()}} together
with \code{\link[=rwisecondorx_newref]{rwisecondorx_newref()}} and \code{\link[=rwisecondorx_predict]{rwisecondorx_predict()}}.

The resulting NPZ has three top-level keys:
\describe{
\item{\code{sample}}{0-d object array wrapping a dict mapping \code{"1"}..\code{"24"}
to int32 arrays (chromosome bin counts).}
\item{\code{binsize}}{Scalar int (bin size in bp).}
\item{\code{quality}}{0-d object array wrapping a dict of QC counters
(populated with zeros since the native binning kernel does not track
per-read filter stats).}
}

When the writer's numpy is >= 2.0 the internal pickle references
\code{numpy._core}, which older numpy (< 2.0) cannot unpickle.  This function
patches the pickle bytestream to use \code{numpy.core} so the NPZ is readable
by both numpy 1.x and 2.x.

Native \code{\link[=bam_convert]{bam_convert()}} returns dense vectors covering the chromosome span in
the BAM header. Upstream \verb{wisecondorx convert} has one additional historical
quirk: it allocates \code{int(length / binsize + 1)} bins for every chromosome in
the header, so chromosomes whose lengths are exact multiples of \code{binsize}
carry one extra trailing all-zero bin. This function pads to that upstream
NPZ layout during serialisation so Python \verb{wisecondorx newref/predict}
remains byte-compatible, while the native R list/BED paths keep the cleaner
header-span contract.
}
\examples{
\dontrun{
bam_convert_npz("sample.bam", "sample.npz", binsize = 5000, rmdup = "streaming")
}

}
\seealso{
\code{\link[=bam_convert]{bam_convert()}}, \code{\link[=bam_convert_bed]{bam_convert_bed()}}, \code{\link[=rwisecondorx_newref]{rwisecondorx_newref()}},
\code{\link[=rwisecondorx_predict]{rwisecondorx_predict()}}, \code{\link[=wisecondorx_newref]{wisecondorx_newref()}}, \code{\link[=wisecondorx_predict]{wisecondorx_predict()}}
}

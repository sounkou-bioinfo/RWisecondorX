% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bam_stats.R
\name{bam_samtools_stats_summary}
\alias{bam_samtools_stats_summary}
\title{Compute samtools-stats-style BAM summary numbers}
\usage{
bam_samtools_stats_summary(
  path,
  reference = NULL,
  min_mapq = 0L,
  require_flags = 0L,
  exclude_flags = 0L,
  decompression_threads = 0L,
  report_filtered_stream_bookkeeping = FALSE
)
}
\arguments{
\item{path}{Path to the input BAM or CRAM file.}

\item{reference}{Optional reference FASTA path for CRAM input when required.}

\item{min_mapq}{Integer; additional minimum MAPQ filter applied before the
samtools-style summary logic. This is a package-level extension for
preprocessing workflows; native \verb{samtools stats} does not expose a direct
MAPQ threshold option.}

\item{require_flags}{Integer; required SAM flag mask, equivalent in spirit
to samtools \code{-f}.}

\item{exclude_flags}{Integer; excluded SAM flag mask, equivalent in spirit
to samtools \code{-F}.}

\item{decompression_threads}{Integer; number of htslib decompression worker
threads to use. \code{0} disables worker threads.}

\item{report_filtered_stream_bookkeeping}{Logical. When \code{FALSE} (default),
\code{raw_total_sequences} stays anchored to the original BAM/CRAM input and
\code{filtered_sequences} reports the number excluded by \code{min_mapq} /
flag-based filtering. When \code{TRUE}, these two fields instead follow the
semantics of \verb{samtools view ... | samtools stats -}, where the retained
stream is treated as the input and \code{filtered_sequences} is therefore \code{0}.}
}
\value{
A one-row \code{data.frame}.
}
\description{
Runs a native htslib-backed summary pass over a BAM or CRAM file and returns
a subset of the summary-number (\code{SN}) fields produced by \verb{samtools stats}.
The core mismatch, duplicate-flag, and CIGAR-denominator logic is ported
from upstream \code{samtools} \code{stats.c}.
}
\details{
\code{error_rate} follows upstream \verb{samtools stats} semantics exactly:
\code{mismatches_from_nm / bases_mapped_cigar}, where mismatches are accumulated
from the \code{NM} auxiliary tag and \code{bases_mapped_cigar} counts \code{M}, \code{I}, \code{=},
and \code{X} CIGAR operations.

\code{reads_duplicated} and the derived duplicate fractions are duplicate-flag
summaries. They are \strong{not} Picard \code{PERCENT_DUPLICATION}.

For exact CLI comparison when using \code{min_mapq}, compare against a pipeline of
the form \verb{samtools view -u -q ... | samtools stats -}.
}

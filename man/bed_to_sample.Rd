% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bed_reader.R
\name{bed_to_sample}
\alias{bed_to_sample}
\title{Read a WisecondorX-format BED file into a sample list}
\usage{
bed_to_sample(bed, binsize = NULL, con = NULL)
}
\arguments{
\item{bed}{Path to a bgzipped (or plain) BED file.}

\item{binsize}{Optional integer; bin size in base pairs. If \code{NULL} (default),
inferred from the first row of the BED file.}

\item{con}{Optional open DBI connection with duckhts already loaded.}
}
\value{
A named list with one integer vector per chromosome key
(\code{"1"}--\code{"22"}, \code{"23"} for X, \code{"24"} for Y). Each vector has length
\code{max_bin + 1} for that chromosome. Chromosomes absent from the BED file
are \code{NULL}. This is the same format returned by \code{\link[=bam_convert]{bam_convert()}}.
}
\description{
Reads a 4-column bgzipped BED file (as written by \code{\link[=bam_convert_bed]{bam_convert_bed()}}) and
returns a named list of integer vectors suitable for \code{\link[=rwisecondorx_newref]{rwisecondorx_newref()}},
\code{\link[=rwisecondorx_predict]{rwisecondorx_predict()}}, \code{\link[=scale_sample]{scale_sample()}}, or \code{\link[=bam_convert_npz]{bam_convert_npz()}}.
}
\details{
The BED file must have 4 tab-delimited columns: \code{chrom}, \code{start}, \code{end},
\code{count} (no header). Coordinates are 0-based half-open intervals. The bin
size is inferred from the first row (\code{end - start}) unless explicitly
provided.
}
\examples{
\dontrun{
# Write bin counts to BED, then read them back
bam_convert_bed("sample.bam", "sample.bed.gz", binsize = 5000L)
bins <- bed_to_sample("sample.bed.gz")

# Use directly with the native WisecondorX pipeline
samples <- lapply(bed_files, bed_to_sample)
ref <- rwisecondorx_newref(samples, binsize = 100000L, nipt = TRUE)
}

}
\seealso{
\code{\link[=bam_convert_bed]{bam_convert_bed()}}, \code{\link[=bam_convert]{bam_convert()}}, \code{\link[=rwisecondorx_newref]{rwisecondorx_newref()}},
\code{\link[=rwisecondorx_predict]{rwisecondorx_predict()}}, \code{\link[=bed_to_nipter_sample]{bed_to_nipter_sample()}}
}

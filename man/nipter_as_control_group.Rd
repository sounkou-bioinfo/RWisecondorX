% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nipter_control.R
\name{nipter_as_control_group}
\alias{nipter_as_control_group}
\title{Build a NIPTeR control group from a list of binned samples}
\usage{
nipter_as_control_group(
  samples,
  description = "General control group",
  sample_sex = NULL,
  sex_source = NULL
)
}
\arguments{
\item{samples}{A list of \code{NIPTeRSample} objects.}

\item{description}{Label for the group (default
\code{"General control group"}).}

\item{sample_sex}{Optional character vector of known sex labels for the
control samples. Accepted values are \code{"female"}, \code{"male"},
\code{"ambiguous"}, and \code{"unknown"}. When unnamed, values are matched
in sample order; when named, names must match \code{sample_name}.}

\item{sex_source}{Optional string describing where \code{sample_sex} came
from, e.g. \code{"explicit"}, \code{"consensus_gmm"}, or
\code{"laboratory_lims"}.}
}
\value{
An object of class \code{c("NIPTeRControlGroup", <strand_type>)}.
}
\description{
Collects multiple \code{NIPTeRSample} objects into a
\code{NIPTeRControlGroup}, the reference cohort used by
\code{\link{nipter_z_score}}, \code{\link{nipter_ncv_score}},
\code{\link{nipter_chi_correct}}, and \code{\link{nipter_regression}}.
}
\details{
All samples must share the same strand type (\code{"CombinedStrands"} or
\code{"SeparatedStrands"}). Duplicate sample names are silently removed.
}
\examples{
\dontrun{
samples <- lapply(bam_files, nipter_bin_bam)
cg <- nipter_as_control_group(samples)
}

}
\seealso{
\code{\link[=nipter_diagnose_control_group]{nipter_diagnose_control_group()}}, \code{\link[=nipter_match_control_group]{nipter_match_control_group()}},
\code{\link[=nipter_z_score]{nipter_z_score()}}, \code{\link[=nipter_chi_correct]{nipter_chi_correct()}}
}

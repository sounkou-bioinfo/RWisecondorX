% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nipter_gaunosome_score.R
\name{nipter_build_gaunosome_models}
\alias{nipter_build_gaunosome_models}
\title{Build both gaunosome model families on a typed reference}
\usage{
nipter_build_gaunosome_models(
  reference,
  candidate_chromosomes = c(1:12, 14:16, 20, 22),
  ncv_min_elements = 6L,
  ncv_max_elements = 9L,
  regression_n_models = 4L,
  regression_n_predictors = 4L,
  regression_extra_predictors = "gc_read_perc_post",
  focus_chromosomes = c("X", "Y")
)
}
\arguments{
\item{reference}{A \code{NIPTReferenceModel}.}

\item{candidate_chromosomes}{Integer vector of autosomes allowed in the
denominator/predictor pool. Defaults to the production-style set
\code{c(1:12, 14:16, 20, 22)}.}

\item{ncv_min_elements}{Minimum denominator-set size for the NCV search.}

\item{ncv_max_elements}{Maximum denominator-set size for the NCV search.}

\item{regression_n_models}{Number of regression models to build per
sex/focus combination.}

\item{regression_n_predictors}{Maximum predictors per regression model.}

\item{regression_extra_predictors}{Optional character vector of additional
numeric columns to use when present in \code{reference$reference_frame}.}

\item{focus_chromosomes}{Character vector; any subset of \code{c("X", "Y")}.}
}
\value{
The input \code{NIPTReferenceModel}, enriched with both
\code{$sex_ncv_models} and \code{$sex_regression_models}.
}
\description{
Convenience wrapper that attaches both sex-chromosome NCV models and
sex-chromosome regression models to a \code{\link{NIPTReferenceModel}}.
This is the package-owned precomputation step for downstream X/Y scoring.
}

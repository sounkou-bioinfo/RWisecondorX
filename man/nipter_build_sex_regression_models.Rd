% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nipter_gaunosome_models.R
\name{nipter_build_sex_regression_models}
\alias{nipter_build_sex_regression_models}
\title{Build sex-chromosome regression models on a typed reference}
\usage{
nipter_build_sex_regression_models(
  reference,
  candidate_chromosomes = c(1:12, 14:16, 20, 22),
  n_models = 4L,
  n_predictors = 4L,
  extra_predictors = "GCPCTAfterFiltering",
  focus_chromosomes = c("X", "Y")
)
}
\arguments{
\item{reference}{A \code{NIPTReferenceModel}.}

\item{candidate_chromosomes}{Integer vector of autosomes allowed as ratio
predictors. Defaults to the production-style set
\code{c(1:12, 14:16, 20, 22)}.}

\item{n_models}{Number of models to build per sex/focus combination.}

\item{n_predictors}{Maximum predictors per model.}

\item{extra_predictors}{Optional character vector of additional numeric
columns to use when present in \code{reference$reference_frame}. The
default \code{"GCPCTAfterFiltering"} keeps room for explicit QC metadata
without hard-wiring that requirement into the core sample classes.}

\item{focus_chromosomes}{Character vector; any subset of \code{c("X", "Y")}.}
}
\value{
The input \code{NIPTReferenceModel}, with a typed
\code{$sex_regression_models} field containing nested
\code{female}/\code{male} and \code{X}/\code{Y} model lists.
}
\description{
Fits multiple sex-matched X/Y ratio models on the non-outlier subsets of a
\code{\link{NIPTReferenceModel}}. The returned reference gains a typed
\code{$sex_regression_models} field.
}

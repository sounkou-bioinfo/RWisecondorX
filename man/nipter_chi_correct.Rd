% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nipter_chi.R
\name{nipter_chi_correct}
\alias{nipter_chi_correct}
\title{Chi-squared correction for overdispersed bins}
\usage{
nipter_chi_correct(
  sample,
  control_group,
  chi_cutoff = 3.5,
  include_sex = FALSE
)
}
\arguments{
\item{sample}{A \code{NIPTeRSample} object (the test sample).}

\item{control_group}{A \code{NIPTeRControlGroup} object.}

\item{chi_cutoff}{Normalised chi-squared threshold. Bins with
\code{(chi - df) / sqrt(2*df) > chi_cutoff} are corrected. Default
\code{3.5}.}

\item{include_sex}{Logical; correct sex chromosomes as well? Default
\code{FALSE}. Sex chromosome bins use the same chi-squared weights
derived from autosomes.}
}
\value{
A list with two elements:
\describe{
\item{sample}{The corrected \code{NIPTeRSample}.}
\item{control_group}{The corrected \code{NIPTeRControlGroup}.}
}
}
\description{
Identifies bins in the control group with excess variance (overdispersion)
using a chi-squared test and downweights them. This reduces the influence
of noisy bins on downstream Z-scores and NCV calculations.
}
\details{
The correction is applied simultaneously to both the test sample and all
control group samples, maintaining consistency.

The algorithm follows NIPTeR's chi-squared correction:
\enumerate{
\item For each control sample, scale autosomal bin counts so that total
reads match the overall mean across all control samples.
\item Compute the expected count per bin (mean of scaled counts).
\item Compute chi-squared per bin:
\eqn{\chi^2 = \sum_i (expected - scaled_i)^2 / expected}.
\item Normalise: \eqn{z = (\chi^2 - df) / \sqrt{2 \cdot df}} where
\eqn{df = n_{controls} - 1}.
\item For bins where \eqn{z > \text{chi\_cutoff}}: divide reads by
\eqn{\chi^2 / df}.
}
}
\examples{
\dontrun{
result <- nipter_chi_correct(sample, control_group)
sample_corrected <- result$sample
cg_corrected     <- result$control_group
}

}
\seealso{
\code{\link[=nipter_gc_correct]{nipter_gc_correct()}}, \code{\link[=nipter_z_score]{nipter_z_score()}},
\code{\link[=nipter_as_control_group]{nipter_as_control_group()}}
}

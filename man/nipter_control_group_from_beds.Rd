% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nipter_control.R
\name{nipter_control_group_from_beds}
\alias{nipter_control_group_from_beds}
\title{Load a NIPTeR control group from a directory of TSV.bgz files}
\usage{
nipter_control_group_from_beds(
  bed_dir,
  pattern = "*.bed.gz",
  binsize = NULL,
  description = "General control group",
  sample_sex = NULL,
  sex_source = NULL,
  con = NULL
)
}
\arguments{
\item{bed_dir}{Character; directory containing one \code{.bed.gz} or
\code{.tsv.bgz} file per control sample, each produced by
\code{\link{nipter_bin_bam_bed}}.}

\item{pattern}{Glob pattern for file discovery (default
\code{"*.bed.gz"}).}

\item{binsize}{Optional integer; bin size in base pairs. If \code{NULL}
(default), inferred from the first row of the first file.}

\item{description}{Label for the resulting control group (default
\code{"General control group"}).}

\item{sample_sex}{Optional character vector of known sex labels for the
samples in \code{bed_dir}. Names must match the inferred sample names when
supplied as a named vector.}

\item{sex_source}{Optional string describing the provenance of
\code{sample_sex}.}

\item{con}{Optional open DBI connection with duckhts loaded.}
}
\value{
A \code{NIPTeRControlGroup}.
}
\description{
Reads all \code{.bed.gz} (or \code{.tsv.bgz}) files in \code{bed_dir} using
\code{rduckhts_tabix_multi()} — a single multi-file DuckDB scan — and
constructs a \code{NIPTeRControlGroup} from the results. This is much faster
than \code{lapply(files, bed_to_nipter_sample)} for large cohorts because all
files are read in one pass.
}
\details{
The column count (5 or 9) is detected automatically from the first file:
5-column BEDs produce a \code{CombinedStrands} control group;
9-column BEDs (written by \code{nipter_bin_bam_bed(separate_strands = TRUE)})
produce a \code{SeparatedStrands} control group. All files in the directory
must have the same column count.
}
\examples{
\dontrun{
# Bin all controls to BED once
for (bam in bam_files) {
  nipter_bin_bam_bed(bam, file.path("controls/", sub(".bam$", ".bed.gz", basename(bam))))
}
# Load them all at once
cg <- nipter_control_group_from_beds("controls/")
}

}
\seealso{
\code{\link[=nipter_bin_bam_bed]{nipter_bin_bam_bed()}}, \code{\link[=nipter_as_control_group]{nipter_as_control_group()}},
\code{\link[=bed_to_nipter_sample]{bed_to_nipter_sample()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nipter_gaunosome_report.R
\name{nipter_gaunosome_report}
\alias{nipter_gaunosome_report}
\title{Score multiple samples against a typed gaunosome reference}
\usage{
nipter_gaunosome_report(
  samples,
  reference,
  y_unique_ratios = NULL,
  min_controls = 2L,
  sample_predictors = NULL,
  focus_chromosomes = c("X", "Y")
)
}
\arguments{
\item{samples}{A non-empty list of \code{NIPTeRSample}/\code{NIPTSample}
objects, or a \code{NIPTControlGroup} whose \code{$samples} should be
scored.}

\item{reference}{A \code{NIPTReferenceModel} prepared with
\code{\link{nipter_build_gaunosome_models}}.}

\item{y_unique_ratios}{Optional named numeric vector keyed by sample name,
used when the reference includes a \code{"y_unique"} sex model.}

\item{min_controls}{Minimum number of non-outlier same-sex controls required
for the z-score component. Default \code{2L}.}

\item{sample_predictors}{Optional named list keyed by sample name. Each
value should be a named list of extra regression predictors for that
sample, used when the fitted models include extra columns such as
\code{gc_read_perc_post}.}

\item{focus_chromosomes}{Character vector; any subset of \code{c("X", "Y")}.}
}
\value{
A typed \code{NIPTGaunosomeReport}.
}
\description{
Batch wrapper around \code{\link{nipter_gaunosome_score}}. Scores each
sample against one \code{\link{NIPTReferenceModel}} and returns a typed
cohort-level report with both the per-sample objects and one flattened
summary table.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nipter_gaunosome_score.R
\name{nipter_gaunosome_score}
\alias{nipter_gaunosome_score}
\title{Score gaunosomes from a typed reference model}
\usage{
nipter_gaunosome_score(
  sample,
  reference,
  y_unique_ratio = NULL,
  min_controls = 2L,
  sample_predictors = NULL,
  focus_chromosomes = c("X", "Y")
)
}
\arguments{
\item{sample}{A \code{NIPTeRSample} or typed \code{NIPTSample}.}

\item{reference}{A \code{NIPTReferenceModel} prepared with
\code{\link{nipter_build_gaunosome_models}}.}

\item{y_unique_ratio}{Optional numeric scalar passed through to
\code{\link{nipter_predict_sex}} when the reference includes a
\code{"y_unique"} sex model.}

\item{min_controls}{Minimum number of non-outlier same-sex controls required
for the z-score component. Default \code{2L}.}

\item{sample_predictors}{Optional named list of extra predictor values for
the sample, used when the regression models include extra columns such as
\code{GCPCTAfterFiltering}.}

\item{focus_chromosomes}{Character vector; any subset of \code{c("X", "Y")}.}
}
\value{
A typed \code{NIPTGaunosomeScore} object containing the component
sex, NCV, and regression scores plus a compact summary data frame.
}
\description{
Computes the package-level X/Y scoring bundle against a prepared
\code{\link{NIPTReferenceModel}}: sex-matched z-scores, sex-matched NCV
scores, and sex-matched regression scores. The result is returned as one
validated object with a compact per-chromosome summary table.
}

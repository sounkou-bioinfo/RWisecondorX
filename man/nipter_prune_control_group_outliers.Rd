% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nipter_control.R
\name{nipter_prune_control_group_outliers}
\alias{nipter_prune_control_group_outliers}
\title{Iteratively prune aberrant controls using chi-correction diagnostics}
\usage{
nipter_prune_control_group_outliers(
  control_group,
  sample = NULL,
  chi_cutoff = 3.5,
  collapse_strands = FALSE,
  z_cutoff = 3,
  max_aberrant_chromosomes = 2L,
  min_controls = 10L,
  max_iterations = 100L,
  verbose = FALSE
)
}
\arguments{
\item{control_group}{A \code{NIPTeRControlGroup}.}

\item{sample}{Optional \code{NIPTeRSample} used as the dummy sample passed
into \code{\link[=nipter_chi_correct]{nipter_chi_correct()}}. Defaults to the first control sample.}

\item{chi_cutoff}{Numeric chi-correction cutoff passed to
\code{\link[=nipter_chi_correct]{nipter_chi_correct()}}. Default \code{3.5}.}

\item{collapse_strands}{Logical; keep 44 strand-resolved diagnostics for
\code{SeparatedStrands} (\code{FALSE}, default) or collapse to 22
autosomal fractions.}

\item{z_cutoff}{Absolute Z-score cutoff passed to
\code{\link[=nipter_diagnose_control_group]{nipter_diagnose_control_group()}}.}

\item{max_aberrant_chromosomes}{Maximum number of distinct aberrant
chromosomes allowed before a sample is dropped. Default \code{2L}.}

\item{min_controls}{Minimum allowed retained control count. The iteration
stops before dropping samples if doing so would leave fewer controls than
this threshold.}

\item{max_iterations}{Maximum number of pruning iterations. Default
\code{100L}.}

\item{verbose}{Logical; emit per-iteration messages.}
}
\value{
A named list with:
\describe{
\item{control_group}{The final uncorrected control group after dropping
flagged samples.}
\item{chi_corrected_control_group}{The final chi-corrected control group
used for the terminal diagnostic pass.}
\item{diagnostics}{The terminal diagnostic list from
\code{\link[=nipter_diagnose_control_group]{nipter_diagnose_control_group()}}.}
\item{dropped_samples}{Character vector of unique dropped sample names in
drop order.}
\item{iteration_log}{Data frame recording flagged and retained control
counts per iteration.}
\item{converged}{Logical; \code{TRUE} when the loop ended without flagged
samples.}
\item{stop_reason}{Text reason for the stopping condition.}
}
}
\description{
Mirrors the legacy production cleaning loop for NIPTeR control cohorts:
chi-correct the current control group, diagnose chromosomal-fraction
outliers, drop flagged samples, and repeat until no further outliers remain
or the minimum control size would be violated.
}
\details{
For \code{SeparatedStrands} control groups with
\code{collapse_strands = FALSE} (default), the pruning rule matches the
legacy script: drop a sample if both strands of any chromosome are
aberrant, or if more than \code{max_aberrant_chromosomes} distinct
chromosomes are aberrant.
}
\seealso{
\code{\link[=nipter_chi_correct]{nipter_chi_correct()}}, \code{\link[=nipter_diagnose_control_group]{nipter_diagnose_control_group()}},
\code{\link[=nipter_drop_control_group_samples]{nipter_drop_control_group_samples()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nipter_regression.R
\name{nipter_regression}
\alias{nipter_regression}
\title{Regression-based Z-score for trisomy prediction}
\usage{
nipter_regression(
  sample,
  control_group,
  chromo_focus,
  n_models = 4L,
  n_predictors = 4L,
  exclude_chromosomes = c(13L, 18L, 21L),
  include_chromosomes = NULL,
  train_fraction = 0.6,
  overdispersion_rate = 1.15,
  force_practical_cv = FALSE,
  seed = NULL
)
}
\arguments{
\item{sample}{A \code{NIPTeRSample} object.}

\item{control_group}{A \code{NIPTeRControlGroup} object.}

\item{chromo_focus}{Integer; the target chromosome (1-22).}

\item{n_models}{Number of regression models to build (default 4).}

\item{n_predictors}{Maximum number of predictor chromosomes per model
(default 4).}

\item{exclude_chromosomes}{Integer vector of chromosomes to exclude from
the predictor pool (default \code{c(13, 18, 21)}).}

\item{include_chromosomes}{Integer vector of chromosomes to force-include.}

\item{train_fraction}{Fraction of control samples used for training
(default 0.6).}

\item{overdispersion_rate}{Overdispersion multiplier for theoretical CV
(default 1.15).}

\item{force_practical_cv}{Logical; always use practical CV regardless of
theoretical CV? Default \code{FALSE}.}

\item{seed}{Random seed for the train/test split. Default \code{NULL}
(no seed set).}
}
\value{
A list of class \code{"NIPTeRRegression"} with elements:
\describe{
\item{models}{A list of \code{n_models} sublists, each containing:
\code{z_score} (numeric), \code{cv} (selected CV), \code{cv_type}
(\code{"practical"} or \code{"theoretical"}), \code{predictors}
(character vector of predictor chromosomes), \code{shapiro_p_value},
\code{control_z_scores} (named numeric vector).}
\item{focus_chromosome}{Character.}
\item{correction_status}{Character vector.}
\item{sample_name}{Character.}
}
}
\description{
Implements NIPTeR's stepwise linear regression approach. Builds multiple
regression models that predict the focus chromosome's fraction from
other chromosomal fractions, then computes Z-scores based on the
prediction residuals.
}
\details{
Currently only supports \code{CombinedStrands} samples. The
\code{SeparatedStrands} variant (which doubles the predictor pool to
forward/reverse per chromosome) will be added when
\code{separate_strands = TRUE} support is implemented in
\code{\link{nipter_bin_bam}}.

The algorithm:
\enumerate{
\item Split the control group 60/40 into train and test sets.
\item Compute chromosomal fractions for all samples.
\item For each of \code{n_models} models, greedily select \code{n_predictors}
chromosomes by maximising adjusted R-squared (forward stepwise
selection). Predictors already used in previous models are excluded.
\item For each model, fit \code{lm(focus ~ predictors)} on the training set,
predict on the test set, and compute:
\itemize{
\item Practical CV: \code{sd(obs/pred) / mean(obs/pred)} on the test set.
\item Theoretical CV: \code{overdispersion_rate / sqrt(total_reads_focus)}
for the test sample.
\item The larger CV is used (unless \code{force_practical_cv = TRUE}).
\item Sample Z-score: \code{(sample_ratio - 1) / cv}.
}
}
}
\examples{
\dontrun{
reg21 <- nipter_regression(sample, cg, chromo_focus = 21)
reg21$models[[1]]$z_score
}

}
\seealso{
\code{\link[=nipter_z_score]{nipter_z_score()}}, \code{\link[=nipter_ncv_score]{nipter_ncv_score()}}
}

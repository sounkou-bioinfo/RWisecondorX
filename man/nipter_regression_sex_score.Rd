% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nipter_gaunosome_models.R
\name{nipter_regression_sex_score}
\alias{nipter_regression_sex_score}
\title{Score sex chromosomes with regression models from a typed reference}
\usage{
nipter_regression_sex_score(
  sample,
  reference,
  focus_chromosome = c("X", "Y"),
  y_unique_ratio = NULL,
  sample_predictors = NULL
)
}
\arguments{
\item{sample}{A \code{NIPTeRSample} or typed \code{NIPTSample}.}

\item{reference}{A \code{NIPTReferenceModel} with \code{$sex_regression_models}
already built by \code{\link{nipter_build_sex_regression_models}}.}

\item{focus_chromosome}{Character scalar, \code{"X"} or \code{"Y"}.}

\item{y_unique_ratio}{Optional scalar passed through to
\code{\link{nipter_predict_sex}} if the reference carries a
\code{"y_unique"} sex model.}

\item{sample_predictors}{Optional named list of extra predictor values for
the sample, used when the fitted regression models include extra columns
such as \code{GCPCTAfterFiltering}.}
}
\value{
A typed \code{NIPTSexRegressionScore}.
}
\description{
Applies the sex-matched X/Y regression models attached to a
\code{NIPTReferenceModel}. Each per-model score is the standardized ratio
of observed to predicted sex-chromosome fraction, mirroring the production
pipeline's regression-based z-score idea.
}

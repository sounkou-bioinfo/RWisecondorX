% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nipter_bin.R
\name{nipter_sample_to_bed}
\alias{nipter_sample_to_bed}
\title{Write an in-memory NIPTeRSample to a bgzipped BED file}
\usage{
nipter_sample_to_bed(
  sample,
  bed,
  binsize,
  corrected = NULL,
  con = NULL,
  index = TRUE,
  metadata = NULL
)
}
\arguments{
\item{sample}{A \code{NIPTeRSample} object (\code{CombinedStrands} or
\code{SeparatedStrands}) from \code{\link[=nipter_bin_bam]{nipter_bin_bam()}}.}

\item{bed}{Path for the output \code{.bed.gz} file.}

\item{binsize}{Bin size in base pairs used when \code{sample} was created.
Required to reconstruct genomic coordinates.}

\item{corrected}{Optional GC-corrected \code{NIPTeRSample} from
\code{\link[=nipter_gc_correct]{nipter_gc_correct()}}. Populated corrected-count columns when supplied;
those columns are \code{NA} otherwise.}

\item{con}{Optional open DBI connection with duckhts already loaded.}

\item{index}{Logical; write a tabix index alongside the BED (default \code{TRUE}).}

\item{metadata}{Optional named list or named atomic vector of provenance
metadata to write as leading \verb{##RWX_<key>=<value>} lines before the BED
body. The data rows remain headerless. Default \code{NULL} writes no metadata.}
}
\value{
\code{bed} (invisibly).
}
\description{
Serialises an already-binned \code{NIPTeRSample} — optionally GC-corrected — to
a bgzipped, tabix-indexed BED file without re-reading any BAM.
}
\details{
Use this when you have already called \code{\link[=nipter_bin_bam]{nipter_bin_bam()}} and optionally
\code{\link[=nipter_gc_correct]{nipter_gc_correct()}}: the BAM is read exactly once and the result is
written in one step.  For a single one-shot BAM → BED conversion with no
intermediate sample object, see \code{\link[=nipter_bin_bam_bed]{nipter_bin_bam_bed()}}.

Column layout mirrors \code{\link[=nipter_bin_bam_bed]{nipter_bin_bam_bed()}}:
\itemize{
\item \strong{CombinedStrands} (default): 5 columns —
\code{chrom}, \code{start}, \code{end}, \code{count}, \code{corrected_count}.
\item \strong{SeparatedStrands} (\code{separate_strands = TRUE}): 9 columns —
\code{chrom}, \code{start}, \code{end}, \code{count}, \code{count_fwd}, \code{count_rev},
\code{corrected_count}, \code{corrected_fwd}, \code{corrected_rev}.
}
}
\examples{
\dontrun{
# Bin once, write — no GC correction
s <- nipter_bin_bam("sample.bam", binsize = 50000L)
nipter_sample_to_bed(s, "sample.bed.gz", binsize = 50000L)

# Bin once, GC-correct, write — BAM read only once
s    <- nipter_bin_bam("sample.bam", binsize = 50000L)
corr <- nipter_gc_correct(s, gc_table = "hg38_gc_50k.tsv.bgz")
nipter_sample_to_bed(s, "sample.bed.gz", binsize = 50000L, corrected = corr)
}

}
\seealso{
\code{\link[=nipter_bin_bam]{nipter_bin_bam()}}, \code{\link[=nipter_bin_bam_bed]{nipter_bin_bam_bed()}}, \code{\link[=nipter_gc_correct]{nipter_gc_correct()}},
\code{\link[=bed_to_nipter_sample]{bed_to_nipter_sample()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rwisecondorx_newref.R
\name{rwisecondorx_newref}
\alias{rwisecondorx_newref}
\title{Build a WisecondorX reference from binned samples}
\usage{
rwisecondorx_newref(
  samples = NULL,
  binsize = 100000L,
  sample_binsizes = NULL,
  nipt = FALSE,
  refsize = 300L,
  yfrac = NULL,
  gender_model_names = "V",
  female_partition_min_samples = 5L,
  male_partition_min_samples = 5L,
  mask_min_median_coverage_fraction = 0.05,
  gender_model_grid_min = 0,
  gender_model_grid_max = 0.02,
  gender_model_grid_length = 5000L,
  pca_components = 5L,
  pca_distance_min_class_bins = 10L,
  pca_distance_autosome_mad_multiplier = 20,
  pca_distance_autosome_floor = 10,
  pca_distance_chrX_mad_multiplier = 20,
  pca_distance_chrX_floor = 10,
  pca_distance_chrY_mad_multiplier = 50,
  pca_distance_chrY_floor = 15,
  null_ratio_max_samples = 100L,
  cpus = 4L,
  bed_dir = NULL,
  bed_pattern = "*.bed.gz",
  con = NULL
)
}
\arguments{
\item{samples}{List of sample objects, each a named list of integer vectors
keyed by chromosome (\code{"1"}--\code{"24"}), as returned by \code{\link[=bam_convert]{bam_convert()}}.
At least 10 samples are required. Mutually exclusive with \code{bed_dir}.}

\item{binsize}{Integer; the target bin size in base pairs. All samples are
rescaled to this size. Default \code{100000L}. Inferred from BED files when
\code{bed_dir} is supplied and \code{sample_binsizes} is \code{NULL}.}

\item{sample_binsizes}{Optional integer vector of per-sample bin sizes. If
\code{NULL} (default), all samples are assumed to already be at \code{binsize}.}

\item{nipt}{Logical; if \code{TRUE}, NIPT mode (no gender correction, no male
gonosomal reference). Default \code{FALSE}.}

\item{refsize}{Integer; number of reference bin locations per target bin.
Default \code{300L}.}

\item{yfrac}{Optional numeric; manual Y-fraction cutoff for gender
classification. If \code{NULL} (default), the cutoff is derived from a GMM.}

\item{gender_model_names}{Character scalar; mclust covariance model name used
for the Y-fraction Gaussian mixture during gender-model fitting. Default
\code{"V"}, matching the current upstream WisecondorX implementation.}

\item{female_partition_min_samples}{Integer; minimum female samples required
before applying female-specific masking and building the female gonosomal
partition. In NIPT mode this is also the minimum female count required to
proceed. Default \code{5L}.}

\item{male_partition_min_samples}{Integer; minimum male samples required
before applying male-specific masking and building the male gonosomal
partition in non-NIPT mode. Default \code{5L}.}

\item{mask_min_median_coverage_fraction}{Numeric scalar; bins with summed
normalized coverage below this fraction of the global median are masked
before model fitting. Default \code{0.05}.}

\item{gender_model_grid_min}{Numeric scalar; lower bound of the density grid
used to locate the Y-fraction cutoff when \code{yfrac} is not provided.
Default \code{0}.}

\item{gender_model_grid_max}{Numeric scalar; upper bound of that density
grid. Default \code{0.02}.}

\item{gender_model_grid_length}{Integer; number of grid points used to
locate the Y-fraction cutoff. Default \code{5000L}.}

\item{pca_components}{Integer; PCA components retained during between-sample
normalization. Default \code{5L}.}

\item{pca_distance_min_class_bins}{Integer; minimum masked bins required
before applying class-specific PCA-distance pruning. Default \code{10L}.}

\item{pca_distance_autosome_mad_multiplier}{Numeric; MAD multiplier used for
autosomal PCA-distance pruning. Default \code{20}.}

\item{pca_distance_autosome_floor}{Numeric; minimum autosomal PCA-distance
cutoff. Default \code{10}.}

\item{pca_distance_chrX_mad_multiplier}{Numeric; MAD multiplier used for
chrX PCA-distance pruning. Default \code{20}.}

\item{pca_distance_chrX_floor}{Numeric; minimum chrX PCA-distance cutoff.
Default \code{10}.}

\item{pca_distance_chrY_mad_multiplier}{Numeric; MAD multiplier used for
chrY PCA-distance pruning. Default \code{50}.}

\item{pca_distance_chrY_floor}{Numeric; minimum chrY PCA-distance cutoff.
Default \code{15}.}

\item{null_ratio_max_samples}{Integer; maximum number of cohort samples used
when sampling columns for null-ratio estimation. Default \code{100L}, matching
the current upstream implementation.}

\item{cpus}{Integer; number of threads for reference bin finding.
Default \code{4L}.}

\item{bed_dir}{Optional character; path to a directory of 4-column bgzipped
BED files (as written by \code{\link[=bam_convert_bed]{bam_convert_bed()}}). All files matching
\code{bed_pattern} are loaded in a single DuckDB pass via
\code{rduckhts_tabix_multi()}. Mutually exclusive with \code{samples}.}

\item{bed_pattern}{Glob pattern for matching BED files inside \code{bed_dir}.
Default \code{"*.bed.gz"}.}

\item{con}{Optional existing DuckDB connection. Used only when \code{bed_dir} is
supplied. A temporary connection is created (and closed) if \code{NULL}.}
}
\value{
A list-like \code{WisecondorXReference} S7 object containing autosomal
and (optionally) gonosomal sub-references. See Details for the full
structure.
}
\description{
Native R implementation of the WisecondorX \code{newref} pipeline. Takes a
list of binned samples (as returned by \code{\link[=bam_convert]{bam_convert()}} or loaded from NPZ
via reticulate) and builds a PCA-based reference suitable for
\code{\link[=rwisecondorx_predict]{rwisecondorx_predict()}}.
}
\details{
The pipeline trains a gender model (2-component GMM on Y-fractions),
optionally applies gender correction for non-NIPT workflows, computes a
global bin mask, then builds three sub-references: autosomes (A), female
gonosomes (F), and male gonosomes (M). Each sub-reference includes PCA
components, within-sample reference bin indices and distances, and null
ratios for between-sample Z-scoring.

This is a faithful port of the upstream Python \verb{wisecondorx newref},
crediting the original WisecondorX authors.

The returned reference object contains:
\describe{
\item{binsize}{Integer; the reference bin size.}
\item{is_nipt}{Logical; whether NIPT mode was used.}
\item{trained_cutoff}{Numeric; Y-fraction gender cutoff.}
\item{has_female}{Logical; whether a female gonosomal reference exists.}
\item{has_male}{Logical; whether a male gonosomal reference exists.}
\item{algorithm_params}{Named list of native reference-building
parameters used to create the object.}
\item{mask, bins_per_chr, masked_bins_per_chr, masked_bins_per_chr_cum,
pca_components, pca_mean, indexes, distances, null_ratios}{Autosomal
reference components.}
\item{mask.F, ..., null_ratios.F}{Female gonosomal reference (if present).}
\item{mask.M, ..., null_ratios.M}{Male gonosomal reference (if present).}
}
}
\seealso{
\code{\link[=rwisecondorx_predict]{rwisecondorx_predict()}}, \code{\link[=scale_sample]{scale_sample()}}, \code{\link[=bam_convert]{bam_convert()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rwisecondorx_predict.R
\name{rwisecondorx_predict}
\alias{rwisecondorx_predict}
\title{Predict copy number aberrations using WisecondorX}
\usage{
rwisecondorx_predict(
  sample,
  reference,
  sample_binsize = NULL,
  outprefix = NULL,
  minrefbins = 150L,
  maskrepeats = 5L,
  alpha = 1e-04,
  zscore = 5,
  optimal_cutoff_sd_multiplier = 3,
  within_sample_mask_iterations = 3L,
  within_sample_mask_quantile = 0.99,
  cbs_split_min_gap_bp = 2000000L,
  segment_zscore_cap = 1000,
  beta = NULL,
  blacklist = NULL,
  gender = NULL,
  seed = NULL,
  parallel = TRUE,
  cpus = 4L
)
}
\arguments{
\item{sample}{Named list of integer vectors keyed by chromosome
(\code{"1"}--\code{"24"}), as returned by \code{\link[=bam_convert]{bam_convert()}}.}

\item{reference}{A \code{WisecondorXReference} object from \code{\link[=rwisecondorx_newref]{rwisecondorx_newref()}},
or a list with equivalent structure.}

\item{sample_binsize}{Integer; bin size of the input sample. Used for
rescaling to the reference bin size.}

\item{outprefix}{Character; path prefix for output files. BED and statistics
files will be written as \verb{<outprefix>_bins.bed}, \verb{<outprefix>_segments.bed},
\verb{<outprefix>_aberrations.bed}, \verb{<outprefix>_statistics.txt}.
If \code{NULL} (default), no files are written and results are returned as a list.}

\item{minrefbins}{Integer; minimum reference bins per target. Bins with
fewer references are zeroed out. Default \code{150L}.}

\item{maskrepeats}{Integer; number of iterative distance-masking cycles.
Default \code{5L}.}

\item{alpha}{Numeric; CBS breakpoint p-value threshold. Default \code{1e-4}.}

\item{zscore}{Numeric; Z-score cutoff for aberration calling. Default \code{5}.}

\item{optimal_cutoff_sd_multiplier}{Numeric; number of population standard
deviations added to the mean distance when iteratively deriving the
optimal within-reference cutoff. Default \code{3}.}

\item{within_sample_mask_iterations}{Integer; number of iterative
within-sample masking passes. Default \code{3L}.}

\item{within_sample_mask_quantile}{Numeric scalar in \verb{(0, 1)}; bins with
\code{|z| >= qnorm(within_sample_mask_quantile)} are excluded from the
next within-sample normalization pass. Default \code{0.99}.}

\item{cbs_split_min_gap_bp}{Integer; NA stretches spanning more than this
many base pairs trigger CBS segment splitting. Default \code{2000000L}.}

\item{segment_zscore_cap}{Numeric; absolute cap applied to segment z-scores
after they are derived from the null-ratio distributions. Default \code{1000}.}

\item{beta}{Optional numeric; if given, ratio-based cutoff is used instead
of Z-score. Should approximate purity (0, 1]. Default \code{NULL}.}

\item{blacklist}{Optional character; path to a headerless BED file of
regions to mask.}

\item{gender}{Optional character; force gender (\code{"F"} or \code{"M"}).}

\item{seed}{Optional integer; RNG seed for CBS reproducibility.}

\item{parallel}{Logical; use \code{ParDNAcopy::parSegment()} for CBS when
\code{TRUE} (default). Requires the \code{ParDNAcopy} package. Set
\code{parallel = FALSE} to use \code{DNAcopy::segment()} explicitly.}

\item{cpus}{Integer; number of threads for parallel CBS (\code{parSegment}) and
any other OpenMP-accelerated steps. Default \code{4L}.}
}
\value{
A list with class \code{"WisecondorXPrediction"} containing:
\describe{
\item{results_r}{List of per-chromosome log2-ratio vectors.}
\item{results_z}{List of per-chromosome Z-score vectors.}
\item{results_w}{List of per-chromosome weight vectors.}
\item{results_c}{Data frame of CBS segments (\code{chr}, \code{start}, \code{end},
\code{zscore}, \code{ratio}).}
\item{aberrations}{Data frame of called aberrations.}
\item{statistics}{Data frame of per-chromosome statistics.}
\item{gender}{Predicted (or forced) gender.}
\item{algorithm_params}{Named list of native prediction parameters used
for this result.}
\item{n_reads}{Total read count.}
\item{binsize}{Reference bin size.}
}
}
\description{
Native R implementation of the WisecondorX \code{predict} pipeline. Takes a
binned sample and a reference (from \code{\link[=rwisecondorx_newref]{rwisecondorx_newref()}}) and detects
copy number aberrations using within-sample normalization, PCA correction,
and circular binary segmentation (CBS).
}
\details{
CBS is performed using the DNAcopy Bioconductor package (or optionally
ParDNAcopy for parallel segmentation). Both must be available in
\code{Suggests}. Unlike \code{\link[=wisecondorx_predict]{wisecondorx_predict()}}, this function's \code{parallel}
argument controls native R CBS execution through \code{ParDNAcopy::parSegment()};
the upstream Python CLI wrapper delegates segmentation to upstream
WisecondorX and does not expose this native switch.
}
\seealso{
\code{\link[=rwisecondorx_newref]{rwisecondorx_newref()}}, \code{\link[=scale_sample]{scale_sample()}}
}

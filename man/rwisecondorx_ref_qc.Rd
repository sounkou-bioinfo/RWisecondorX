% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rwisecondorx_ref_qc.R
\name{rwisecondorx_ref_qc}
\alias{rwisecondorx_ref_qc}
\title{QC a native WisecondorX reference}
\usage{
rwisecondorx_ref_qc(reference, min_ref_bins = 150L, output_json = NULL)
}
\arguments{
\item{reference}{A \code{WisecondorXReference} object from
\code{\link[=rwisecondorx_newref]{rwisecondorx_newref()}}.}

\item{min_ref_bins}{Integer; minimum number of usable normalizing reference
bins retained for a target bin before the report flags a warning. This is
not the number of cohort samples. Default \code{150L}, matching the upstream
Python QC heuristic.}

\item{output_json}{Optional path for writing the QC report as JSON. When
supplied, requires the \code{jsonlite} package.}
}
\value{
A named list with class \code{"WisecondorXReferenceQC"} containing:
\describe{
\item{overall_verdict}{\code{"PASS"}, \code{"WARN"}, or \code{"FAIL"}.}
\item{worst_severity}{Integer severity code: \code{0L} pass, \code{1L} warn, \code{2L}
fail.}
\item{compat_issues}{Character vector of autosomal-prefix compatibility
issues between autosomal and gonosomal sub-references.}
\item{metrics}{Named list of per-branch metrics (\code{A}, \code{F}, \code{M}) including
verdict, message, mean/std of per-bin mean distances, outlier counts, and
counts of target bins that fall below the normalizing-reference-bin
threshold. Male reports also include chrY metrics.}
}
}
\description{
Native R implementation of the upstream WisecondorX \code{ref_qc.py} heuristics
for \code{WisecondorXReference} objects. Inspects within-sample reference-bin
distances, compatibility between autosomal and gonosomal mask prefixes, and
(for male references) chrY usability metrics.
}
\details{
The function returns a structured QC report and can optionally write the
report as JSON for later inspection.
}
\seealso{
\code{\link[=rwisecondorx_newref]{rwisecondorx_newref()}}, \code{\link[=rwisecondorx_predict]{rwisecondorx_predict()}}
}

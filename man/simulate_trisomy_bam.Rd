% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_bam.R
\name{simulate_trisomy_bam}
\alias{simulate_trisomy_bam}
\title{Simulate a trisomy BAM by thinning a donor alignment}
\usage{
simulate_trisomy_bam(
  bam,
  out_bam,
  trisomy_chr,
  fetal_fraction = 0.1,
  mosaic = FALSE,
  seed = 1L,
  threads = 1L,
  reference = NULL,
  index = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{bam}{Input BAM or CRAM path.}

\item{out_bam}{Output BAM path.}

\item{trisomy_chr}{Target chromosome to enrich, e.g. \code{"21"} or \code{"chr21"}.}

\item{fetal_fraction}{Numeric fetal fraction in \verb{(0, 1]}. Default \code{0.10}.}

\item{mosaic}{Logical; if \code{TRUE}, use the 50\% mosaic enrichment model
(\code{k = 0.25}) instead of the non-mosaic model (\code{k = 0.5}).}

\item{seed}{Integer RNG seed for deterministic thinning.}

\item{threads}{Integer thread count for htslib I/O and BAM indexing.}

\item{reference}{Optional FASTA path required when the input is CRAM.}

\item{index}{Logical; if \code{TRUE} (default), create a \code{.bai} index for
\code{out_bam}.}

\item{overwrite}{Logical; overwrite \code{out_bam} and its \code{.bai} if present.}
}
\value{
A named list with input/output record counts and thinning metadata.
}
\description{
Creates a BAM by copying all reads on the target chromosome unchanged and
randomly removing reads from all other mapped chromosomes according to the
Nguyen et al. fetal-fraction model. This preserves the donor alignment's
read names, flags, CIGAR strings, mapping qualities, and coordinate order
while enriching the target chromosome relative to the rest of the genome.
}
\details{
This is a BAM-level simulation primitive intended for realistic fixture and
benchmark generation from real negative donor alignments. It is more faithful
than the compressed synthetic cohort generator in \code{\link[=generate_cohort]{generate_cohort()}}, but it
is still a simulation and does not replace real-data validation.
}
\examples{
\dontrun{
simulate_trisomy_bam(
  bam = "negative_sample.bam",
  out_bam = "simulated_t21.bam",
  trisomy_chr = "21",
  fetal_fraction = 0.12,
  seed = 42L
)
}

}
\seealso{
\code{\link[=generate_cohort]{generate_cohort()}}, \code{\link[=bam_convert]{bam_convert()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_trisomy_cohort.R
\name{simulate_trisomy_cohort}
\alias{simulate_trisomy_cohort}
\title{Simulate a trisomy cohort from donor BAMs/CRAMs}
\usage{
simulate_trisomy_cohort(
  bams = NULL,
  bam_dir = NULL,
  out_dir,
  donor_ids = NULL,
  trisomy_chromosomes = c("21", "18", "13"),
  fetal_fraction = 0.1,
  mosaic = FALSE,
  seed = 1L,
  threads = 1L,
  reference = NULL,
  index = TRUE,
  include_donors = TRUE,
  pattern = "\\\\.(bam|cram)$",
  overwrite = FALSE
)
}
\arguments{
\item{bams}{Optional character vector of donor BAM/CRAM paths.}

\item{bam_dir}{Optional directory containing donor BAM/CRAM files.
Supply exactly one of \code{bams} or \code{bam_dir}.}

\item{out_dir}{Output directory for simulated positive BAMs and the manifest.}

\item{donor_ids}{Optional donor IDs, one per input BAM/CRAM. Defaults to
filename stems with duplicates made unique.}

\item{trisomy_chromosomes}{Character vector of target chromosomes.
Default \code{c("21", "18", "13")}.}

\item{fetal_fraction}{Numeric vector of fetal fractions. A separate
simulation is produced for each distinct value.}

\item{mosaic}{Logical vector of mosaic settings. A separate simulation is
produced for each distinct value. Default \code{FALSE}.}

\item{seed}{Integer base seed. Individual simulations use deterministic
seeds derived from this base.}

\item{threads}{Integer thread count for htslib I/O and indexing.}

\item{reference}{Optional FASTA path applied to all CRAM inputs.}

\item{index}{Logical; create \code{.bai} indexes for simulated BAMs. Default
\code{TRUE}.}

\item{include_donors}{Logical; include the donor negatives as rows in the
manifest. Default \code{TRUE}.}

\item{pattern}{File-matching regex used only when \code{bam_dir} is supplied.}

\item{overwrite}{Logical; overwrite existing simulated BAMs and manifest.}
}
\value{
A data frame manifest. The same table is written to
\verb{<out_dir>/manifest.tsv}.
}
\description{
Generates a set of positive BAMs from one or more negative donor alignments
by applying \code{\link[=simulate_trisomy_bam]{simulate_trisomy_bam()}} across a simulation grid. The result is
a manifest that ties every simulated BAM back to its donor, chromosome,
fetal fraction, and mosaic setting.
}
\details{
The donor BAMs themselves serve as the negative cohort; by default they are
recorded in the manifest but not copied into \code{out_dir}.
}
\examples{
\dontrun{
manifest <- simulate_trisomy_cohort(
  bam_dir = "negatives/",
  out_dir = "simulated_positives/",
  trisomy_chromosomes = c("21", "18"),
  fetal_fraction = c(0.08, 0.12)
)
}

}
\seealso{
\code{\link[=simulate_trisomy_bam]{simulate_trisomy_bam()}}, \code{\link[=generate_cohort]{generate_cohort()}}
}

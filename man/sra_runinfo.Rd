% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa.R
\name{sra_runinfo}
\alias{sra_runinfo}
\alias{sra_runinfo_url}
\alias{download_sra_runinfo}
\alias{read_sra_runinfo}
\title{SRA Run Metadata Utilities}
\usage{
sra_runinfo_url(accession, mode = c("acc", "term"))

download_sra_runinfo(
  accession,
  dest = file.path("inst", "extdata", paste0(accession, "_runinfo.csv")),
  overwrite = FALSE,
  quiet = TRUE,
  mode = c("acc", "term"),
  downloader = utils::download.file
)

read_sra_runinfo(
  accession,
  path = system.file("extdata", paste0(accession, "_runinfo.csv"), package =
    "RWisecondorX")
)
}
\arguments{
\item{accession}{A study or project accession such as \code{PRJNA400134}.}

\item{mode}{Retrieval mode. \code{"acc"} queries the backend with \verb{acc=<accession>};
\code{"term"} queries with \verb{term=<accession>}.}

\item{dest}{Output CSV path. Defaults to \verb{inst/extdata/<accession>_runinfo.csv}
in the current source checkout.}

\item{overwrite}{Logical; overwrite an existing file.}

\item{quiet}{Logical; passed through to the downloader.}

\item{downloader}{Function used to retrieve the URL. Defaults to
\code{\link[utils:download.file]{utils::download.file()}}. This is injectable so tests can avoid network
calls.}
}
\value{
\code{sra_runinfo_url()} returns a length-1 character URL.
\code{download_sra_runinfo()} returns the output path, invisibly.
\code{read_sra_runinfo()} returns a \code{data.frame}.
}
\description{
These helpers standardize how \code{RWisecondorX} stages SRA run metadata for
conformance fixtures. They use the NCBI SRA backend \code{runinfo} endpoint and
default to writing project metadata into \verb{inst/extdata/} in the source tree.
}
\details{
The SRA Run Selector and backend \code{runinfo} endpoints can expose slightly
different columns. For reproducible package fixtures, \code{RWisecondorX}
standardizes on the backend \code{runinfo} CSV output.
}
\examples{
sra_runinfo_url("PRJNA400134")

\dontrun{
download_sra_runinfo("PRJNA400134")
metadata <- read_sra_runinfo("PRJNA400134")
}

}

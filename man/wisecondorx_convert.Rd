% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wisecondorx_cli.R
\name{wisecondorx_convert}
\alias{wisecondorx_convert}
\title{Convert BAM/CRAM to WisecondorX NPZ format (upstream CLI wrapper)}
\usage{
wisecondorx_convert(
  bam,
  npz,
  reference = NULL,
  binsize = 5000L,
  normdup = FALSE,
  env_name = "wisecondorx",
  extra_args = character(0)
)
}
\arguments{
\item{bam}{Path to an indexed BAM or CRAM file.}

\item{npz}{Path for the output \code{.npz} file (created or overwritten).}

\item{reference}{Optional path to a FASTA reference file. Required when
\code{bam} is a CRAM file. Passed to upstream \code{--reference}.}

\item{binsize}{Bin size in base pairs (default 5000). The reference bin
size should be a multiple of this value. Passed to upstream \code{--binsize}.}

\item{normdup}{Logical; when \code{TRUE}, passes \code{--normdup} to skip duplicate
removal. Recommended for NIPT data where read depth is low.}

\item{env_name}{Name of the conda environment containing \code{wisecondorx}
(default \code{"wisecondorx"}). Created automatically by condathis on first
use via the \code{bioconda} channel.}

\item{extra_args}{Character vector of additional arguments passed verbatim
after the mapped CLI flags. For forward compatibility with future upstream
WisecondorX releases.}
}
\value{
\code{npz} (invisibly).
}
\description{
Calls \verb{wisecondorx convert} via \code{\link[condathis:run]{condathis::run()}} to convert an aligned
BAM or CRAM file to a \code{.npz} file for downstream use with
\code{\link[=wisecondorx_newref]{wisecondorx_newref()}} or \code{\link[=wisecondorx_predict]{wisecondorx_predict()}}.
}
\details{
This wrapper exposes the upstream \verb{wisecondorx convert} CLI flags:
\code{--reference}, \code{--binsize}, and \code{--normdup}.

For a fully native R implementation (no Python dependency) that uses
Rduckhts instead of pysam, see \code{\link[=bam_convert]{bam_convert()}} and \code{\link[=bam_convert_npz]{bam_convert_npz()}}.
}
\examples{
\dontrun{
wisecondorx_convert(
  bam = "sample.bam",
  npz = "sample.npz",
  binsize = 5000L,
  normdup = FALSE
)
}

}
\seealso{
\code{\link[=bam_convert]{bam_convert()}}, \code{\link[=bam_convert_npz]{bam_convert_npz()}}, \code{\link[=wisecondorx_newref]{wisecondorx_newref()}},
\code{\link[=wisecondorx_predict]{wisecondorx_predict()}}
}

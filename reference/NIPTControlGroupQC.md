# Control-group QC report

Typed QC object bundling chromosome-level, sample-level, sex-stratified,
and optional chi/bin-level summaries for a `NIPTControlGroup`.

## Usage

``` r
NIPTControlGroupQC(.data)
```

## Arguments

- .data:

  Named list payload for the typed constructor.

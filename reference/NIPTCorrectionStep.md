# Typed correction step in a NIPTeR correction path

Typed correction step in a NIPTeR correction path

## Usage

``` r
NIPTCorrectionStep(code = character(0))
```

## Arguments

- code:

  Correction step code. One of `"raw"`, `"gc"`, or `"chi"`.

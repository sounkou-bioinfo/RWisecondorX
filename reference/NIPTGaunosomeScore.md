# Aggregate gaunosome score report

Typed package-level report bundling sex, NCV, and regression-based X/Y
scoring against a built `NIPTReferenceModel`.

## Usage

``` r
NIPTGaunosomeScore(.data)
```

## Arguments

- .data:

  Named list payload for the typed constructor.

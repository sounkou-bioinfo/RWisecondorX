# Built NIPT reference model

A validated, serialisable package-level reference object that bundles
the control group, chromosome-level reference frame, and sex-prediction
models.

## Usage

``` r
NIPTReferenceModel(.data)
```

## Arguments

- .data:

  Named list payload for the typed constructor.

# WisecondorX binned sample

A list-like S7 object keyed by chromosome `"1"`–`"24"`, where each
element is a numeric vector of bin counts.

## Usage

``` r
WisecondorXSample(.data)
```

## Arguments

- .data:

  Named list payload for the typed constructor.

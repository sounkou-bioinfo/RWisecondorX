# Compute per-chromosome statistics

Mirrors `predict_output._generate_chr_statistics_file()`.

## Usage

``` r
.compute_statistics(
  results_r,
  results_w,
  results_c,
  results_nr,
  bins_per_chr,
  binsize,
  ref_gender,
  gender,
  n_reads
)
```

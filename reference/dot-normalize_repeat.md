# Within-sample normalization with iterative aberration masking (3 iterations)

Within-sample normalization with iterative aberration masking (3
iterations)

## Usage

``` r
.normalize_repeat(
  test_data,
  ref,
  optimal_cutoff,
  ct,
  cp,
  ap,
  iterations = 3L,
  mask_quantile = 0.99
)
```
